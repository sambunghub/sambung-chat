# ===========================================
# DATABASE
# ===========================================
DATABASE_URL=postgresql://user:password@localhost:5432/sambung-chat

# ===========================================
# BETTER AUTH
# ===========================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-to-random-32-char-string
BETTER_AUTH_URL=http://localhost:3000

# ===========================================
# CORS
# ===========================================
CORS_ORIGIN=http://localhost:5173

# ===========================================
# ENCRYPTION (for API keys)
# ===========================================
# Encryption key for securing API keys at rest (AES-256-GCM).
# Must be a 32-byte base64-encoded key (256 bits).
#
# Generate a secure key with one of these methods:
# - OpenSSL: openssl rand -base64 32
# - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# - Bun: bun -e "console.log(crypto.randomBytes(32).toString('base64'))"
# - Helper script: bun run scripts/generate-encryption-key.ts
#
# SECURITY CRITICAL:
# - Never commit this key to git
# - Never share this key or use in multiple environments
# - If you lose this key, all encrypted API keys will be permanently unreadable
# - Backup this key securely before deployment
#
# See docs/setup/api-keys.md for detailed setup instructions
ENCRYPTION_KEY=change-me-to-base64-encoded-32-byte-key

# ===========================================
# SERVER
# ===========================================
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# ===========================================
# OPENAI COMPATIBLE API (Optional)
# ===========================================
# Uncomment if using OpenAI-compatible provider
# OPENAI_API_KEY=your-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4
