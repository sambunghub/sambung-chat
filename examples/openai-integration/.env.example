# OpenAI Provider Configuration
# Copy this file to .env.local and fill in your actual values

# Required: Your OpenAI API key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Specify which model to use
# Options: gpt-4o-mini (default, fast/cheap), gpt-4o (balanced), o1-mini (reasoning), o1-preview (advanced)
OPENAI_MODEL=gpt-4o-mini

# Optional: Your OpenAI organization ID (if you have multiple organizations)
# Find it at: https://platform.openai.com/account/org-settings
# OPENAI_ORGANIZATION=org-your-organization-id

# Optional: Custom base URL (for proxies, Azure deployments, or custom endpoints)
# Most users don't need this
# OPENAI_BASE_URL=https://api.openai.com/v1

# Server configuration
PORT=3001
NODE_ENV=development
