name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

# IMPORTANT: All jobs must pass before merging to develop
# These jobs are required status checks for branch protection

jobs:
  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bun run check-types

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [type-check]
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

  # Test job - optional for now (no tests configured yet)
  # Uncomment when tests are properly set up
  # test:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: [build]
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install
  #
  #     - name: Start database
  #       run: bun run db:start
  #       env:
  #         POSTGRES_DB: test_db
  #         POSTGRES_USER: test_user
  #         POSTGRES_PASSWORD: test_password
  #
  #     - name: Run tests
  #       run: bun test
  #
  #     - name: Stop database
  #       run: bun run db:stop
  #       if: always()
